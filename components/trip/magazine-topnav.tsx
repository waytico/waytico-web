// Magazine theme TopNav — desktop ≥1024px sticky navigation.
//
// Skeleton stub. Wired in Step 3 from hero.tsx (Magazine branch);
// props API is finalised at integration time. File exists so a real
// diff can land on the branch and open the PR.
export function MagazineTopNav() {
  return null
}
